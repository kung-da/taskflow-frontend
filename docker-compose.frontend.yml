services:
  frontend:
    build:
      context: .
      dockerfile: frontend.Dockerfile
      args:
        VITE_API_BASE_URL: http://localhost:4000/api
    ports:
      - '5173:80'
    restart: unless-stopped
